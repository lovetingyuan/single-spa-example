{
  "name": "single-spa-example",
  "version": "1.0.0",
  "private": true,
  "description": "example of single-spa.",
  "scripts": {
    "serve": "npx cross-env NODE_ENV=development node serve-build",
    "build": "npx cross-env NODE_ENV=production node serve-build",
    "build:root": "npx parcel build src/index.html --cache-dir ./node_modules/.cache --no-minify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lovetingyuan/single-spa-example.git"
  },
  "keywords": [
    "single-spa",
    "microfrontends",
    "vue",
    "react"
  ],
  "author": "tingyuan",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lovetingyuan/single-spa-example/issues"
  },
  "homepage": "https://github.com/lovetingyuan/single-spa-example#readme",
  "workspaces": [
    "modules/*"
  ],
  "devDependencies": {
    "@babel/core": "^7.8.7",
    "concurrently": "^5.1.0",
    "cross-env": "^7.0.2",
    "jsdom": "^16.2.1",
    "merge-dirs": "^0.2.1",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "single-spa": "^5.2.1"
  }
}
